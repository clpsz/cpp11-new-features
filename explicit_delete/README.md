#说明
有的对象的拷贝是没有意义的，但是如果不定义的话，编译器自作主张地帮你合成一个默认的拷贝构造函数和拷贝操作符函数。

在以前的版本中，如果希望不能拷贝的话，可以在类中声明拷贝控制函数，但是不定义他们，这样在链接的时候就会报未定义符号的错误。

如果想进一步把报错从链接阶段转到编译阶段，则可以继承一个不能被拷贝的对象，因为在编译派生类的拷贝控制函数时，派生类必须知道父类的拷贝控制函数的定义才行，如果父类就不能拷贝的话，则派生类无法通过编译。

